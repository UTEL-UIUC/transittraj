% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtfs_helpers.R
\name{project_onto_route}
\alias{project_onto_route}
\title{Projects points to linear distances along a route shape.}
\usage{
project_onto_route(
  shape_geometry,
  points,
  original_crs = 4326,
  project_crs = 4326
)
}
\arguments{
\item{shape_geometry}{The SF object to project onto. Must include the field
\code{shape_id}. See \code{get_shape_geometry()}.}

\item{points}{Can be either: a dataframe representing point coordinates,
with fields \code{longitude} and \code{latitude}; or, an SF or SFC point object.}

\item{original_crs}{Optional. A numeric EPSG identifier. If a dataframe is
provided for \code{points}, this will be used to define the coordinate system of
the longitude / latitude values. Default is 4326 (WGS 84 ellipsoid).}

\item{project_crs}{Optional. A numeric EPSG identifer indicating the
coordinate system to use for spatial calculations. Consider setting to a
Euclidian projection, such as the appropriate UTM zone. Default is 4326 (WGS
84 ellipsoid).}
}
\value{
The \code{points} input (either dataframe or SF) with an appended column
for the linear distance along the route. If \code{points} is an SFC, a vector of
numeric distances is returned. Units are those of the spatial projection
used (e.g., meters if using UTM).
}
\description{
This function takes spatial points and projects them onto a route, returning
the linear distance from the beginning terminal of the route.
}
